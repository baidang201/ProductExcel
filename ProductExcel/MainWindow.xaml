<Window x:Class="ProductExcel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="1024" Width="768">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="800"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button Grid.Row="1" Grid.Column="1" x:Name="btnProductThree" Content="生成3天" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,3" Width="150" Height="50" Click="btnProductThree_Click"></Button>
        <Button Grid.Row="1" Grid.Column="2" x:Name="btnProductSix" Content="生成6天" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,3" Width="150" Height="50" Click="btnProductSix_Click"></Button>
        <Button Grid.Row="1" Grid.Column="3" x:Name="btnAddARow" HorizontalAlignment="Center" VerticalAlignment="Center" Content="添加一行数据"     Margin="6,3"  Width="150"  Height="50" Click="btnAddARow_Click"/>
        
        <DataGrid x:Name="dataGridExcel" Grid.Row="2"  Grid.Column="0" Grid.ColumnSpan="4" HorizontalAlignment="Left"    VerticalAlignment="Top" AutoGenerateColumns="False" RowHeight="24"  Height="800" Width="750" >
            <DataGrid.Columns>
                <DataGridTextColumn Width="140" Binding="{Binding Path=Name,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Header="姓名"   />
                <DataGridTextColumn Width="140" Binding="{Binding Path=PayExt,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Header="手续费" />
                <DataGridTextColumn Width="180" Binding="{Binding Path=BillDay,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Header="账单日" />
                <DataGridTextColumn Width="100" Binding="{Binding Path=PayDay,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Header="还款日" />
                <DataGridTextColumn Width="100" Binding="{Binding Path=PayLimit,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Header="额度" />
            </DataGrid.Columns>
        </DataGrid>




    </Grid>
</Window>
